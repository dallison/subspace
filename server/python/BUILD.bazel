load("@pybind11_bazel//:build_defs.bzl", "pybind_extension")

pybind_extension(
    name = "subspace_server",
    srcs = ["server.cc"],
    visibility = ["//visibility:public"],
    deps = [
        "//server",
        "@coroutines//co:co",
    ],
)
